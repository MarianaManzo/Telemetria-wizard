        {/* SaveRuleModal */}
        <SaveRuleModal
          isOpen={showSaveModal}
          onClose={() => setShowSaveModal(false)}
          onSave={handleSaveRule}
          defaultData={{
            name: ruleName,
            description: ruleDescription
          }}
        />
      </div>
    </TooltipProvider>
  )
}